import 'package:flutter/material.dart';

class RadioScren extends StatelessWidget {
  static const String routname = 'RadioScren';

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
      child: Text('mmmm'),
    );
  }
}
